datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Students {
  rollno String @unique
  name String
  gender String
  class String
  bloodgrp String
}